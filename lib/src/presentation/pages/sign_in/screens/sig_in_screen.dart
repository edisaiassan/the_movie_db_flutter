import 'package:flutter/material.dart';

class SigInScreen extends StatelessWidget {
  const SigInScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SigInScreen'),
      ),
    );
  }
}